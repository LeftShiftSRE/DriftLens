# This repo may live on a filesystem without symlink/hardlink support (e.g.
# exFAT on Windows external drives). These settings make pnpm copy packages —
# including local workspace packages — instead of linking them.
node-linker=hoisted
inject-workspace-packages=true
package-import-method=copy
